# python

Every pull request is reviewed automatically by an AI reviewer (local Ollama mistral + language rules) via .github/workflows/ai-review.yml. Findings are inline comments with one-click Apply suggestion fixes where possible. Suggestions only - nothing changes without a human accepting it.
